*** cudf-dist/python/cudf/setup.py	2020-05-03 18:44:07.036438111 +0900
--- cudf/python/cudf/setup.py	2020-05-03 18:45:43.259016442 +0900
***************
*** 32,37 ****
--- 32,38 ----
      )
  
  cuda_include_dir = os.path.join(CUDA_HOME, "include")
+ cuda_library_dir = os.path.join(CUDA_HOME, "lib64")
  
  try:
      nthreads = int(os.environ.get("PARALLEL_LEVEL", "0") or "0")
***************
*** 52,59 ****
              np.get_include(),
              cuda_include_dir,
          ],
!         library_dirs=[get_python_lib(), os.path.join(os.sys.prefix, "lib")],
!         libraries=["cudf"],
          language="c++",
          extra_compile_args=["-std=c++14"],
      )
--- 53,60 ----
              np.get_include(),
              cuda_include_dir,
          ],
!         library_dirs=[get_python_lib(), os.path.join(os.sys.prefix, "lib"),cuda_library_dir],
!         libraries=["cudf","cuda","cudart"],
          language="c++",
          extra_compile_args=["-std=c++14"],
      )
